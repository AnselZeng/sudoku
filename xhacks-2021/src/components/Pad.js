import React, { Component } from 'react';

export default class Pad extends Component {
    state = {
        mode: 'default',

    }

    changeMode(button) {
        this.setState(
            
        )
    }

    render() { 
        return (
        <div>
            <button>1</button>
        </div>);
    }
}

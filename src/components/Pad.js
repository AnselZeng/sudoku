import React, { Component } from 'react';

export default class Pad extends Component {
    state = {
        mode = 'default',

    }

    changeMode(button) {
        switch(button){
            
        }
    }

    render() { 
        return (
        <div>
            <button>1</button>
        </div>);
    }
}
